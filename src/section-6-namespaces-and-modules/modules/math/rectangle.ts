/*
DEFAULT keyword will make this the object to be imported
when no other object is specified.
*/
export default function calculateRectangle(width: number, length: number) {
    return width * length;
}